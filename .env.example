MONGODB_URI="mongodb+srv://<username>:<password>@<cluster>/<database>?retryWrites=true&w=majority"
# Use apenas se a rede local não resolver registros SRV do MongoDB Atlas.
MONGODB_DNS_SERVER=""

# Usado somente para criar o primeiro administrador com `npm run seed:admin`.
BOOTSTRAP_ADMIN_EMAIL=""
BOOTSTRAP_ADMIN_PASSWORD=""
BOOTSTRAP_TENANT_ID="novus-publico-inicial"
